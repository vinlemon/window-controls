import { css } from "lit";

export const buttonStyles = css`
  button {
    display: inline-flex;
    cursor: default;
    align-items: center;
    justify-content: center;
    border: none;
    padding: 0;
  }
`;
